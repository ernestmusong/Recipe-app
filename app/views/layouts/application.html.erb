<!DOCTYPE html>
<html>
  <head>
    <title>Recipe App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body>
    <header>
      <nav class="nav">
        <%= link_to 'Public recipes', public_recipes_path, class: 'nav_link'%>
        <% if user_signed_in? %>
          <%= link_to 'Foods', user_foods_path(current_user), method: :get, class: 'nav_link' %>
          <%= link_to "New food", new_user_food_path(current_user), method: :get, class: 'nav_link'%>
          <%= link_to 'Recipes', user_recipes_path(current_user), class: 'nav_link' %>
          <%= link_to "New recipe", new_user_recipe_path(current_user), class: 'nav_link' %>
          <%= link_to 'Shopping list', user_shopping_lists_path(current_user), class: 'nav_link' %>
          <%= link_to "Logout", sign_out_user_path, method: :get, class: 'nav_link' %>
          <span class="nav_link"> Logged in as <strong><%= current_user.email %></strong></span>.
        <% else %>
          <%= link_to "Sign up", new_user_registration_path, :class => 'nav_link'  %>
          <%= link_to "Login", new_user_session_path, :class => 'nav_link'  %>
        <% end %>
         
      </nav>
   </header>
   <div class="notice_wrap">
        <p class="notice"><%= notice %></p>
        <p class="alert"><%= alert %></p>
      </div>
    <%= yield %>
  </body>
</html>
