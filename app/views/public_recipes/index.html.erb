<main>
    <h1>Public Recipes</h1>
    <% if @recipes.blank? %>
    <p>There are not any public recipes at the moment</p>
    <% else %>
    <ul>
    <% @recipes.each do |recipe| %>
      <%= link_to user_recipe_path(recipe.user.id, recipe), class: "link" do %>
        <li class="public_list_item">
            <div class="item">
              <h3><%= recipe.name %></h3>
              <h4>By: <%= recipe.user.name %></h4>
            </div>
            <div class="item">
                <p>total food items: <%= recipe.user.foods.count %></p>
                <p>total price: $<%= recipe.user.foods.sum { |food| food.price * food.quantity } %></p>
            </div>
        </li>
      <%end%>
    <% end %>
    </ul>
    <% end %>
</main>